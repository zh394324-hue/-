<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة التحكم - FitBoard Dashboard</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        * {
            font-family: 'Cairo', sans-serif;
        }
        :root {
            --lime: #84CC16;
            --lime-light: #BEF264;
            --lime-dark: #65A30D;
            --lime-glow: #A3E635;
        }
        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1e 50%, #0a0a0a 100%);
        }
        .neon-glow {
            box-shadow: 0 0 20px rgba(132, 204, 22, 0.4), 0 0 40px rgba(132, 204, 22, 0.2);
        }
        .neon-text {
            text-shadow: 0 0 10px rgba(132, 204, 22, 0.8), 0 0 20px rgba(132, 204, 22, 0.5);
        }
        .stat-card {
            background: linear-gradient(145deg, #1a1a1e 0%, #0f0f12 100%);
            border: 1px solid #2a2a2e;
            transition: all 0.3s ease;
        }
        .stat-card:hover {
            border-color: #84CC16;
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(132, 204, 22, 0.1);
        }
        .table-row:hover {
            background: rgba(132, 204, 22, 0.05);
        }
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #1a1a1e;
        }
        ::-webkit-scrollbar-thumb {
            background: #84CC16;
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #65A30D;
        }
        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="min-h-screen text-white">
    
    <!-- Dashboard Header -->
    <header class="py-4 px-6 border-b border-gray-800 bg-black/50 backdrop-blur-sm sticky top-0 z-50">
        <div class="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4">
            <div class="text-center sm:text-right">
                <h1 class="text-2xl font-black text-[#84CC16] neon-text">لوحة مبيعات FITGEAR</h1>
                <p class="text-xs text-gray-500 tracking-widest">Management Dashboard v2.0</p>
            </div>
            <div class="flex items-center gap-3">
                <button onclick="exportToCSV()" class="flex items-center gap-2 bg-[#84CC16]/20 border border-[#84CC16]/50 text-[#84CC16] px-4 py-2 rounded-lg text-sm hover:bg-[#84CC16]/30 transition-all duration-300">
                    <span>📊</span>
                    <span>تصدير للإكسل (CSV)</span>
                </button>
                <a href="index.html" class="flex items-center gap-2 bg-gray-800 text-gray-300 px-4 py-2 rounded-lg text-sm hover:bg-gray-700 transition-all duration-300">
                    <span>🏠</span>
                    <span>الرئيسية</span>
                </a>
            </div>
        </div>
    </header>

    <div class="p-6 max-w-7xl mx-auto">
        
        <!-- Stats Cards -->
        <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8 fade-in">
            
            <!-- Visits Card -->
            <div class="stat-card rounded-2xl p-5 relative overflow-hidden">
                <div class="flex items-start justify-between">
                    <div>
                        <p class="text-gray-400 text-sm mb-1">حركة الزوار</p>
                        <p class="text-4xl font-black text-white" id="stat-visits">0</p>
                        <p class="text-xs text-gray-500 mt-2">إجمالي الزيارات</p>
                    </div>
                    <div class="w-14 h-14 bg-blue-500/20 rounded-xl flex items-center justify-center text-3xl">
                        👁️
                    </div>
                </div>
                <div class="absolute -bottom-6 -left-6 w-28 h-28 bg-blue-500/5 rounded-full"></div>
            </div>

            <!-- Orders Card -->
            <div class="stat-card rounded-2xl p-5 relative overflow-hidden">
                <div class="flex items-start justify-between">
                    <div>
                        <p class="text-gray-400 text-sm mb-1">الطلبات المؤكدة</p>
                        <p class="text-4xl font-black text-[#84CC16] neon-text" id="stat-orders">0</p>
                        <p class="text-xs text-gray-500 mt-2">طلبات جديدة</p>
                    </div>
                    <div class="w-14 h-14 bg-[#84CC16]/20 rounded-xl flex items-center justify-center text-3xl">
                        🛒
                    </div>
                </div>
                <div class="absolute -bottom-6 -left-6 w-28 h-28 bg-[#84CC16]/5 rounded-full"></div>
            </div>

            <!-- Conversion Rate Card -->
            <div class="stat-card rounded-2xl p-5 relative overflow-hidden">
                <div class="flex items-start justify-between">
                    <div>
                        <p class="text-gray-400 text-sm mb-1">معدل التحويل</p>
                        <p class="text-4xl font-black text-[#BEF264]" id="stat-conversion">0%</p>
                        <p class="text-xs text-gray-500 mt-2">أداء الموقع</p>
                    </div>
                    <div class="w-14 h-14 bg-purple-500/20 rounded-xl flex items-center justify-center text-3xl">
                        📈
                    </div>
                </div>
                <div class="absolute -bottom-6 -left-6 w-28 h-28 bg-purple-500/5 rounded-full"></div>
            </div>

            <!-- Revenue Card -->
            <div class="stat-card rounded-2xl p-5 relative overflow-hidden">
                <div class="flex items-start justify-between">
                    <div>
                        <p class="text-gray-400 text-sm mb-1">المبيعات المتوقعة</p>
                        <p class="text-4xl font-black text-white" id="stat-revenue">0</p>
                        <p class="text-xs text-[#84CC16] mt-2">ريال سعودي</p>
                    </div>
                    <div class="w-14 h-14 bg-green-500/20 rounded-xl flex items-center justify-center text-3xl">
                        💰
                    </div>
                </div>
                <div class="absolute -bottom-6 -left-6 w-28 h-28 bg-green-500/5 rounded-full"></div>
            </div>
        </div>

        <!-- Orders Table Section -->
        <div class="bg-gray-900/50 border border-gray-800 rounded-2xl overflow-hidden fade-in" style="animation-delay: 0.2s;">
            
            <!-- Table Header -->
            <div class="p-5 border-b border-gray-800 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 bg-gray-900/80">
                <div>
                    <h2 class="text-xl font-bold text-[#84CC16]">سجل طلبات العملاء</h2>
                    <p class="text-sm text-gray-500">إدارة ومراجعة بيانات الطلبات الواردة من صفحة الهبوط</p>
                </div>
                <button onclick="clearAllOrders()" class="flex items-center gap-2 bg-red-500/10 border border-red-500/30 text-red-400 px-4 py-2 rounded-lg text-sm hover:bg-red-500/20 transition-all duration-300">
                    <span>🗑️</span>
                    <span>مسح السجل بالكامل</span>
                </button>
            </div>

            <!-- Table -->
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-gray-800/50">
                        <tr class="text-right text-sm text-gray-400">
                            <th class="px-4 py-4 font-semibold">معلومات العميل</th>
                            <th class="px-4 py-4 font-semibold">أرقام التواصل</th>
                            <th class="px-4 py-4 font-semibold">الكمية</th>
                            <th class="px-4 py-4 font-semibold">المنطقة</th>
                            <th class="px-4 py-4 font-semibold">العنوان الوطني والسكن</th>
                            <th class="px-4 py-4 font-semibold">توقيت الطلب</th>
                            <th class="px-4 py-4 font-semibold">إجراءات</th>
                        </tr>
                    </thead>
                    <tbody id="orders-table-body">
                        <!-- Orders will be inserted here -->
                    </tbody>
                </table>
            </div>

            <!-- Empty State -->
            <div id="empty-state" class="hidden p-16 text-center">
                <div class="text-7xl mb-4">📭</div>
                <h3 class="text-2xl font-bold text-gray-300 mb-2">صندوق الطلبات فارغ</h3>
                <p class="text-gray-500">لم يتم تسجيل أي طلبات جديدة حتى الآن.</p>
            </div>
        </div>

        <!-- Footer -->
        <div class="mt-8 text-center text-gray-600 text-sm">
            <p>© 2024 FitBoard - لوحة إدارة المبيعات</p>
        </div>
    </div>

    <!-- Confirm Delete Modal -->
    <div id="confirm-modal" class="hidden fixed inset-0 bg-black/80 backdrop-blur-sm flex items-center justify-center z-50 p-6">
        <div class="bg-gray-900 border border-red-500/50 rounded-2xl p-8 max-w-md w-full text-center fade-in">
            <div class="text-6xl mb-4">⚠️</div>
            <h2 class="text-2xl font-bold text-red-400 mb-2">تأكيد الحذف</h2>
            <p class="text-gray-400 mb-6" id="confirm-message">هل أنت متأكد من حذف هذا العنصر؟</p>
            <div class="flex gap-4 justify-center">
                <button onclick="closeConfirmModal()" class="px-6 py-3 rounded-xl font-bold bg-gray-700 hover:bg-gray-600 transition">
                    إلغاء
                </button>
                <button id="confirm-delete-btn" class="px-6 py-3 rounded-xl font-bold bg-red-500 hover:bg-red-600 transition">
                    حذف
                </button>
            </div>
        </div>
    </div>

    <script>
        // Database Configuration
        const DB_KEY = 'fitgear_dashboard_v2';
        const PRICE = 117;
        
        let db = {
            visits: 0,
            orders: []
        };

        // Load database from localStorage
        function loadDB() {
            try {
                const saved = localStorage.getItem(DB_KEY);
                if (saved) {
                    db = JSON.parse(saved);
                }
            } catch (e) {
                console.error('Error loading database:', e);
            }
        }

        // Save database to localStorage
        function saveDB() {
            try {
                localStorage.setItem(DB_KEY, JSON.stringify(db));
            } catch (e) {
                console.error('Error saving database:', e);
            }
        }

        // Render Dashboard
        function renderDashboard() {
            // Update Stats
            document.getElementById('stat-visits').textContent = db.visits.toLocaleString('ar-SA');
            document.getElementById('stat-orders').textContent = db.orders.length.toLocaleString('ar-SA');
            
            const conversionRate = db.visits > 0 ? ((db.orders.length / db.visits) * 100).toFixed(1) : 0;
            document.getElementById('stat-conversion').textContent = conversionRate + '%';
            
            const totalRevenue = db.orders.reduce((sum, order) => sum + order.total, 0);
            document.getElementById('stat-revenue').textContent = totalRevenue.toLocaleString('ar-SA') + ' ر.س';
            
            // Render Orders Table
            const tableBody = document.getElementById('orders-table-body');
            const emptyState = document.getElementById('empty-state');
            
            if (db.orders.length === 0) {
                tableBody.innerHTML = '';
                emptyState.classList.remove('hidden');
            } else {
                emptyState.classList.add('hidden');
                tableBody.innerHTML = db.orders.slice().reverse().map(order => `
                    <tr class="table-row border-b border-gray-800 text-sm transition-colors duration-200">
                        <td class="px-4 py-4">
                            <div class="font-bold text-white text-base">${order.fullname}</div>
                        </td>
                        <td class="px-4 py-4">
                            <div class="text-[#84CC16] font-semibold">${order.phone}</div>
                            <div class="text-gray-500 text-xs mt-1">${order.phone2 !== '-' ? order.phone2 : 'لا يوجد رقم إضافي'}</div>
                        </td>
                        <td class="px-4 py-4">
                            <span class="bg-[#84CC16]/20 text-[#84CC16] px-4 py-2 rounded-full font-bold text-lg">${order.quantity}</span>
                        </td>
                        <td class="px-4 py-4">
                            <div class="text-gray-300 font-medium">${order.city}</div>
                        </td>
                        <td class="px-4 py-4">
                            <div class="text-gray-300 text-xs max-w-xs">
                                <div class="font-semibold text-[#BEF264] mb-1">${order.national_address}</div>
                                <div class="text-gray-500 leading-relaxed">${order.address}</div>
                            </div>
                        </td>
                        <td class="px-4 py-4">
                            <div class="text-gray-400 text-sm">${order.timestamp}</div>
                        </td>
                        <td class="px-4 py-4">
                            <button onclick="deleteOrder(${order.id})" class="text-red-400 hover:text-red-300 hover:bg-red-500/10 p-3 rounded-xl transition-all duration-200" title="حذف الطلب">
                                🗑️
                            </button>
                        </td>
                    </tr>
                `).join('');
            }
        }

        // Delete Single Order
        function deleteOrder(id) {
            document.getElementById('confirm-message').textContent = 'هل أنت متأكد من حذف هذا الطلب؟';
            document.getElementById('confirm-modal').classList.remove('hidden');
            
            document.getElementById('confirm-delete-btn').onclick = function() {
                db.orders = db.orders.filter(order => order.id !== id);
                saveDB();
                renderDashboard();
                closeConfirmModal();
            };
        }

        // Clear All Orders
        function clearAllOrders() {
            if (db.orders.length === 0) {
                alert('لا توجد طلبات للحذف');
                return;
            }
            
            document.getElementById('confirm-message').textContent = 'هل أنت متأكد من حذف جميع الطلبات؟ لا يمكن التراجع عن هذا الإجراء.';
            document.getElementById('confirm-modal').classList.remove('hidden');
            
            document.getElementById('confirm-delete-btn').onclick = function() {
                db.orders = [];
                saveDB();
                renderDashboard();
                closeConfirmModal();
            };
        }

        // Close Confirm Modal
        function closeConfirmModal() {
            document.getElementById('confirm-modal').classList.add('hidden');
        }

        // Export to CSV
        function exportToCSV() {
            if (db.orders.length === 0) {
                alert('لا توجد طلبات للتصدير');
                return;
            }
            
            const headers = ['الاسم', 'رقم الجوال', 'رقم إضافي', 'المدينة', 'الكمية', 'العنوان الوطني', 'العنوان التفصيلي', 'الإجمالي', 'التاريخ'];
            
            const rows = db.orders.map(order => [
                order.fullname,
                order.phone,
                order.phone2,
                order.city,
                order.quantity,
                order.national_address,
                order.address.replace(/,/g, ' -'),
                order.total + ' ر.س',
                order.timestamp
            ]);
            
            const csvContent = '\uFEFF' + headers.join(',') + '\n' + rows.map(row => row.join(',')).join('\n');
            
            const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });
            const link = document.createElement('a');
            link.href = URL.createObjectURL(blob);
            link.download = 'FitGear_Orders_' + new Date().toISOString().split('T')[0] + '.csv';
            link.click();
        }

        // Close modal on escape key
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                closeConfirmModal();
            }
        });

        // Close modal on backdrop click
        document.getElementById('confirm-modal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeConfirmModal();
            }
        });

        // Initialize
        document.addEventListener('DOMContentLoaded', function() {
            loadDB();
            renderDashboard();
            
            // Auto refresh every 5 seconds
            setInterval(() => {
                loadDB();
                renderDashboard();
            }, 5000);
        });
    </script>
</body>
</html>
